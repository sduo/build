name: 'Tailscale DERP Server'
on:  
  # 手动触发
  workflow_dispatch:
jobs:
  amd64:
    runs-on: ubuntu-24.04 #ubuntu-24.04-arm
    steps:
      # https://github.com/tailscale/tailscale
      - name: '检出 tailscale/tailscale 仓库'
        uses: actions/checkout@v3
        with:
          repository: tailscale/tailscale
          path: tailscale
          submodules: true
      # 
      - name: '初始化 Go 环境'
        uses: actions/setup-go@v6
        with:
          go-version: '1.25.6'
      - name: '检查 Go 版本'
        run: go version
      - name: '初始化编译环境'
        run: mkdir --parents ${GITHUB_WORKSPACE}/builds/derp
      - name: '编译代码'
        run: |          
          cd ${GITHUB_WORKSPACE}/tailscale/derp
          go build -ldflags "-s -w" -o ${GITHUB_WORKSPACE}/builds/derp/derper
      - name: '检查编译目标'
        run: ls -l ${GITHUB_WORKSPACE}/builds/derp
      - name: '权限设置'
        run: chmod 755 ${GITHUB_WORKSPACE}/builds/derp/derper
      - name: '检查编译目标'
        run: ls -l ${GITHUB_WORKSPACE}/builds/derp
      - name: '测试版本'
        run: ${GITHUB_WORKSPACE}/builds/derp/derper --help



    
